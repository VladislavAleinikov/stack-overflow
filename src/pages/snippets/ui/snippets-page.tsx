import { SnippetsList } from "@/widgets/snippets-list";
import { Suspense } from "react";

interface SnippetsPageProps {
  isThisUserPost?: boolean;
}

export const SnippetsPage: React.FC<SnippetsPageProps> = ({
  isThisUserPosts,
}) => {
  return (
    <div>
    </div>
  );
};
